ui = true

backend "consul" {
   address = "127.0.0.1:8500"
   path = "vault"
}

listener "tcp" {
   address = "0.0.0.0:8200"
   cluster_address = "{{ cluster_address }}"
   tls_disable = 1
}

disable_mlock = true
api_addr = "{{ api_addr }}"
cluster_addr = "{{ cluster_addr }}"
